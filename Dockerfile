# 1. Node.js 18 버전의 가벼운(slim) 버전을 기본 재료로 사용합니다.
FROM node:18-slim

# 2. 컨테이너 안에서 작업할 폴더를 만들고 그곳으로 이동합니다.
WORKDIR /usr/src/app

# 3. (효율성을 위한 꿀팁!) 먼저 부품 목록(package.json)만 복사합니다.
# 이렇게 하면 나중에 코드만 바뀌고 부품 목록은 그대로일 때, 불필요한 부품 설치 과정을 생략해서 빌드 속도가 엄청나게 빨라집니다.
COPY package*.json ./

# 4. 부품 목록을 보고 필요한 모든 부품(express 등)을 설치합니다.
RUN npm install

# 5. 나머지 모든 소스 코드(app.js 등)를 복사합니다.
COPY . .

# 6. 이 컨테이너는 8080번 문(포트)을 사용한다고 외부에 알려줍니다.
EXPOSE 8080

# 7. 컨테이너가 시작될 때 최종적으로 실행할 명령어를 지정합니다. (node app.js)
CMD [ "npm", "start" ]